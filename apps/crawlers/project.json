{
  "name": "crawlers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/crawlers/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "crawlers:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "crawlers:build:development"
        },
        "production": {
          "buildTarget": "crawlers:build:production"
        }
      }
    }
  }
}
