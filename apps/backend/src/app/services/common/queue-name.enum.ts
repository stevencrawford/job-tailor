export enum QueueName {
  DataCollectorFetch = 'data-collector.fetch',
  DataCollectorJob = 'data-collector.job',
  JobEnrich = 'job.enrich',
  JobSummarize =  'job.summarize',
  JobCategorize = 'job.categorize',
  CandidateLookup = 'candidate.lookup',
  CandidateMatch = 'candidate.match',
}
